# SafeStep 실행 순서

---

## 팀원 — 처음 세팅 (최초 1회)

### 1. 레포 클론

```powershell
git clone https://github.com/milk152milk/navigation.git
```

---

### 2. Android Studio에서 프로젝트 열기

Android Studio 실행 → **Open** 클릭 → 아래 경로 선택:

```
navigation/준범/SafeStep
```

Gradle 동기화가 자동으로 시작됩니다. 완료까지 1~2분 소요.

---

### 3. 앱 실행

폰을 USB로 연결하고 **개발자 옵션 → USB 디버깅** 활성화 후  
Android Studio 상단 **▶ Run** 버튼 클릭 (단축키: `Shift + F10`)

앱 최초 실행 시 권한 요청이 뜨면 **모두 허용**:
- 카메라
- 마이크
- 위치

---

### 4. 서버 URL 입력

앱 실행 후 홈 화면에서 바로 URL 입력란이 보입니다.  
준범에게 현재 서버 URL을 받아서 입력하세요.

```
예시: https://xxxx-xxxx.ngrok-free.app
```

> 설정 탭 → 서버 설정에서도 언제든지 변경 가능합니다.  
> 코드 수정이나 앱 재빌드는 **필요 없습니다.**

---

### 5. 연결 확인

폰 브라우저에서 아래 주소 접속:

```
https://준범이_알려준_URL/health
```

아래처럼 뜨면 정상:

```json
{"status": "ok", "yolo": "bbox2.pt", "surface": "surface.pt"}
```

---

## 팀원 — 매번 실행할 때

```
1. 준범에게 서버 URL 확인 (ngrok 고정 도메인 설정 시 항상 동일)
2. 앱 실행
3. 홈 화면 URL 입력란에 URL 입력 후 사용
```

---

## 코드 최신화 (팀원)

```powershell
cd navigation
git pull origin main
```

---

---

## 준범 — 서버 실행

### 처음 세팅 (최초 1회)

Python 3.9 이상 필요.

```powershell
pip install fastapi uvicorn ultralytics opencv-python pillow numpy torch pyngrok python-dotenv
```

ngrok 토큰 설정 (`server/.env` 파일 생성):

```
NGROK_AUTH_TOKEN=발급받은_토큰
NGROK_DOMAIN=발급받은_고정도메인.ngrok-free.app
```

> ngrok 토큰/도메인 발급: [dashboard.ngrok.com](https://dashboard.ngrok.com)  
> Domains → New Domain → 무료 고정 도메인 생성  
> 설정하면 서버 재시작해도 URL이 항상 동일 → 팀원 재입력 불필요

---

### 서버 시작

```powershell
cd navigation/준범/SafeStep/server
python server.py
```

정상 시작 시 출력:

```
[SafeStep] YOLO 탐지 로드 완료 ✅
[SafeStep] 노면 세그멘테이션 로드 완료 ✅
============================================================
[SafeStep] 공개 URL (ngrok): https://xxxx-xxxx.ngrok-free.app
============================================================
INFO: Uvicorn running on http://0.0.0.0:8000
```

이 URL을 팀원들에게 공유하세요.

---

### 서버 종료

터미널에서 `Ctrl + C`

터미널이 닫혔거나 응답 없을 때 강제 종료:

```powershell
Get-Process python,ngrok -ErrorAction SilentlyContinue | Stop-Process -Force
```

---

### 방화벽 설정 (연결 안 될 때 최초 1회, 관리자 PowerShell)

```powershell
New-NetFirewallRule -DisplayName "SafeStep Server" -Direction Inbound -Protocol TCP -LocalPort 8000 -Action Allow
```

---

## 준범 — 깃 업데이트

코드 변경 후:

```powershell
cd navigation/준범/SafeStep

git add .
git commit -m "업데이트 내용 간단히 적기"
git push origin main
```
